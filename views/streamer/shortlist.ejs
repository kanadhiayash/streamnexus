<section>
  <div class='page-toolbar'>
    <h1>My List</h1>
    <div class='view-toggle' role='group' aria-label='Choose My List view'>
      <button type='button' data-view='grid' data-view-toggle class='active' aria-pressed='true'>Grid</button>
      <button type='button' data-view='list' data-view-toggle aria-pressed='false'>List</button>
    </div>
  </div>
  <% if (contents.length === 0) { %>
    <div class='empty-state' role='status'>
      <p>My List is empty. Browse titles and save a few for later.</p>
      <a href='/home' class='btn btn-primary' style='margin-top:1rem;'>Browse Titles</a>
    </div>
  <% } else { %>
    <div class='grid'>
      <% contents.forEach(content => { %>
        <div class='card'>
          <% const capacity = content.capacity || { rentalLimit: content.rentalLimit || 20, activeRentals: 0, remaining: content.rentalLimit || 20, isFull: false }; %>
          <img class='content-poster' src='<%= content.image %>' alt='<%= content.title %>' loading='lazy' data-fallback-src='/images/default.svg' />
          <div class='card-content'>
            <h3><%= content.title %></h3>
            <p class='meta-line'><%= content.type.toUpperCase() %> / <%= content.genre || 'General' %></p>
            <div class='price'>$<%= content.price.toFixed(2) %></div>
            <p class='capacity-copy'><%= capacity.remaining %>/<%= capacity.rentalLimit %> licences open</p>
          </div>
          <div class='card-footer'>
            <div class='actions'>
              <a href='/titles/<%= content._id %>' class='btn btn-primary btn-sm' aria-label='View details for <%= content.title %>'>View</a>
              <form method='post' action='/titles/<%= content._id %>/shortlist/remove' style='display:inline;'>
                <input type='hidden' name='_csrf' value='<%= csrfToken %>' />
                <button type='submit' class='btn btn-danger btn-sm' aria-label='Remove <%= content.title %> from My List'>Remove</button>
              </form>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  <% } %>
</section>
