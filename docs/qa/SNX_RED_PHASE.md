# SNX-000 Red Phase

The SNX validation contract tests were added before implementation.

Expected missing pieces during this phase:

- validation registry
- registry validator
- npm SNX aliases
- final SNX CI identifier gate

This file is temporary evidence for the test-first sequence and must be removed before merge.
